-#encoding: utf-8
.row{style: "height:70px;"}
  %h1{style: "display: inline-block;"} 客户端数据
:css
  .table .tv {
    text-align: center;
    vertical-align: middle;
    max-width: 20px;
  }
.row{style: "margin-top: 5px; background: white;"}
  %table.table.table-striped.table-condensed
    %thead
      %tr
        %th 最近三十笔(#{@api_datas.count})
        %th timestamp
    %tbody
      - @api_datas.first(30).each do |data|
        %tr
          %td= data.data
          %td= data.created_at.strftime("%Y-%m-%d %H:%M:%S")

